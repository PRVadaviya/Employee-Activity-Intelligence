version : 2

models:
  - name: raw_attendance
    config:
      materialized: table
  - name: raw_employees_tasks
    config:
      materialized: table
  - name: raw_employees_data
    config:
      materialized: table